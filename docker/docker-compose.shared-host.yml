# ingress only: the proxy that already owns 80 and 443 reaches the service over
# its network, and nothing of this project is published on the host
name: gradus

services:
  backend:
    networks:
      - edge
      - wished-proxy

networks:
  wished-proxy:
    external: true
    name: wished_wished-app
